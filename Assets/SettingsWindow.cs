using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SettingsWindow : MonoBehaviour
{

    /// <summary>
    /// we should have a lot properties here
    /// we should keep tracking all the properties
    /// if properties change, we have to tell user to apply new one or reset it 
    /// </summary>
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
